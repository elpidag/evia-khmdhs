# Pages are crawlable. The document proxy and the JSON API are not: /pdf/
# fetches from ΚΗΜΔΗΣ and Διαύγεια on demand, so a crawler walking those
# links would make this server re-download thousands of documents.
User-agent: *
Disallow: /pdf/
Disallow: /api/
