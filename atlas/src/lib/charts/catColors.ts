/**
 * The TYPE palette (user, 2026-08-22): one colour per curated work-type
 * category, drawn from the site's own inks — the greens of the other
 * pages, the accent, the ΔΑΣΕ deep blue — over a neutral grey for the
 * catch-all mass; semantic where possible (reforestation green,
 * flood-protection deep blue). Shared by the PROCUREMENT TIMELINE's
 * type lens and the TYPES OF WORKS grouped dots, so a category keeps
 * one colour across the page.
 */
export const CAT_COLORS: Record<string, string> = {
	dasotexnika: '#cfcfcf',
	miktes_zones: '#1d1d1d',
	arxaiologikoi: '#b33a1a',
	meletes: '#b07d1e',
	antidiavrotika: '#0d366b',
	anadasoseis: '#52b788',
	ylotomies: '#2d6a4f',
	ydatodexamenes: '#43a276'
};
